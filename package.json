{
  "name": "itpipes-conversion-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Legacy file conversion worker — design review and repair exercise",
  "scripts": {
    "test": "node --experimental-strip-types --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22.6.0"
  }
}
